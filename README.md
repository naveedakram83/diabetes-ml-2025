# diabetes-ml-2025
# CP70066E – Diabetes Risk (Group Project)

## Structure
- `data/` (Blackboard dataset goes here; not committed)
- `notebooks/diabetes_project.ipynb` (all work)
- `figures/` (plots saved from notebook)
- `reports/` (executive summary & ethics)

## How to run
1) `pip install -r requirements.txt`
2) Open `notebooks/diabetes_project.ipynb`
3) Run cells top→bottom

## Goal
Predict `Outcome` (0/1) using diagnostic features; follow Weeks 1–5 workflow.

## Notes
- Use only the Blackboard dataset.
- Keep figures and reports exported to `figures/` and `reports/`.
